{
  "name": "hidook-site-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Hidook Site Builder — commercial product is the browser builder (`npm run build:app`). `npm run build` assembles a sample static site into dist/ for local/dev only.",
  "scripts": {
    "build": "node scripts/build-site.js",
    "gallery": "node scripts/build-gallery.js",
    "build:app": "node scripts/build-builder.js"
  }
}
