# Unmerged branch audit — 2026-09-06/07

Scope: every branch returned by `git branch --no-merged main` at the start of this
audit (26 branches — one more than the 25 quoted in the task; see note at bottom).
Worktree directories for these branches were deleted the previous night; only the
refs remain. This report determines, per branch, whether it holds anything not
already on `main`. **No branch is deleted or modified by this report.**

Method: `git diff --stat main...<branch>` (evidence under `04-QA-Evidence/**`
excluded as noise — it was collapsed on `main` and will show as "deleted" on
every branch), then `git diff main...<branch> -- <file>` per surviving file,
read against what `main` already contains.

Status: COMPLETE. All 26 branches audited: 22 SAFE TO DELETE, 4 HOLDS SOMETHING,
0 UNCLEAR. See "Still wanted?" section after the table for the HOLDS SOMETHING
branches.

| Branch | Verdict | Evidence |
|---|---|---|
| audit/adversarial-1788732502 | SAFE TO DELETE | Root-level FINDINGS.md documents 5 confirmed defects from an adversarial re-audit of commits d73df4d..d2e3a58. All 5 are independently fixed on main, each with a code comment that matches the finding verbatim: (1) touch-target oracle's tautology-defeating shared-selector guard -> fixed in 95373c0 (`bot/test/waveB-touch-targets.test.js` now says *"a guard any template can satisfy on behalf of all of them guards nothing"* and splits `OWN_ACTIONS` per template); (2) professionals hero/cookie-banner collision at 720x450 (200%-zoom) -> fixed in a37503d (`templates/professionals/styles.css` `@media (max-height: 660px)`, comment: *"A laptop at 200% browser zoom reports about 720x450 CSS pixels ... This is the height talking, so key off the height"*); (3) impossible 20.4:1 contrast figure -> corrected to 17.32:1 in `bot/site-legal.js:230`; (4) stale 2.5.8 citation -> fixed in 02a4c54, `bot/test/waveB-consent-control-quality.test.js` now cites 2.5.5; (5) portfolio scroll-hint under cookie banner at 1440px -> fixed in a37503d, `bot/site-legal.js` `@media (min-width: 900px)` rule with comment *"portfolio's 'EXPLOREAZĂ' was measured overlapping the card by 232x21"*. The one SUSPECTED (unresolved) item in the branch's own findings was never confirmed as a real defect. Everything else in the diff is scripts/screenshots under `audit-evidence/` (out of scope, evidence-only). |
| worktree-agent-a210a955143021a0f | HOLDS SOMETHING | `04-QA-Evidence/Reaudit-2026-09-06/commercial-ops/FINDINGS.md` (a detailed adversarial re-audit of payments/deploy-infra, driven over real HTTP). Its two Critical findings (C1: double-subscription via the ordinary edit-and-republish `/api/publish` path, not just the renewal button; C2: dunning state never surfaced to the dashboard) and High H1 (no invoice-history UI) are all CONFIRMED FIXED on main — `bot/server.js:3315-3352` explicitly comments "Wave8 audit (re-audit finding #1 ...)" and now calls `reconcileSiteFromStripe` + `canStartRenewalCheckout` before falling into the unpaid/new-checkout branch; `bot/server.js:1450` attaches `dunning` to every site ("Wave8 audit finding #2"); `builder/app.js` has a "Facturi" button wired to `openInvoicesModal`/`GET /invoices`; `bot/test/wave8-billing-entitlement-agree.test.js` and two `wave8-owner-ui-*-reachable-e2e` tests guard these paths (M2 also resolved). BUT two items are still true on main and NOT written down anywhere else: **(H2)** `BACKUP-RESTORE.md` still says verbatim "This repo does not currently ship a wrapper script for this" (line 64) and "An automated backup script or cron job — none exists in this repo yet" (line 163), while `scripts/ops-backup.js` and `scripts/ops-restore.js` (real, tested, `--yes`-gated, pre-restore-snapshotting CLIs, deliberately kept in the production Docker image per `.dockerignore`) already exist — the doc is stale the day an operator needs it, and the companion doc that described the real procedure (`HANDOFF-ops.md`) is confirmed gone from the repo entirely. **(M1)** `bot/registry-sqlite.js:29` still does `const db = openRegistryDb();` at module load, and `bot/web.js:29` still eagerly `require('./webpublish.js')` (unlike `bot/server.js`'s own lazy `getRegistry()` pattern used elsewhere) — so if `registry.sqlite` is already corrupted when the process boots, `bot/web.js` throws before `startServer()` binds a port and `/health`/`/health/ready` cannot report anything (a materially narrower guarantee than "readiness always tells the truth", though arguably an acceptable fail-fast trade-off given Railway's restart policy). H3 (USD default for a visitor with no `CF-IPCountry` and an `en`-tagged/absent Accept-Language) also still reproduces unchanged in `bot/pricing.js`, by design, and was already flagged as a known, accepted limitation. |
| worktree-agent-a3a347f1a5502f4b2 | SAFE TO DELETE | `04-QA-Evidence/Reaudit-2026-09-06/backend-security/FINDINGS.md` re-audited backend/security at merge-base 7736ed7 and found 3 still-live issues the original audit believed fixed: C1 (no server-side logout/session revocation), C2 (stored XSS in portfolio `{{& icon}}` sink surviving the fix via a named HTML entity `javascript&colon;...` instead of a numeric one), H1 (per-IP rate limiting trivially bypassed by spoofing X-Forwarded-For, no trusted-proxy allowlist). All 3 are now fixed on main, each with a comment explicitly citing this re-audit: logout — `bot/server.js` routes `/api/auth/logout` and `/api/auth/logout-everywhere` ("Wave 8 / AUDIT-07"), `bot/auth.js` `revokeSession`/`revokeAllSessionsForUser`; XSS — `build.js` now decodes named entities too (`colon: ':'` table) before the scheme-blocklist check; X-Forwarded-For — `bot/server.js:142-144` comment: "the re-audit's live-probe.mjs fired 30 requests with 30 spoofed X-Forwarded-For values and got zero blocked", `getClientIp()` no longer trusts the header by default. Its Medium finding (raw `RangeError` leaked to client on deeply-nested JSON) is fixed too — `bot/server.js:3836-3838` comment cites "M1, 2026-09-06 re-audit" and now returns a clean generic message for any un-annotated error. Its Low findings (admin token accepted via `?token=` query string; live-site CSP keeps `'unsafe-inline'`) are addressed/partially addressed (admin token now bootstraps a cookie from the query param rather than accepting it repeatedly) and were self-rated "hygiene note"/"not a standalone vulnerability" by the branch's own author, not worth resurrecting. |
| worktree-agent-a61ab6ab06e1bd9f0 | SAFE TO DELETE | `04-QA-Evidence/Reaudit-2026-09-06/builder-a11y/FINDINGS.md` re-audited builder UX + a11y and found 5 concrete issues, all now fixed on main with matching comments: desserdirina's 9 contrast failures (identical to original A11Y-05, `.section-eyebrow`/`.menu-lang-btn`/`.menu-cat` at 3.2-4.37:1) -> `templates/desserdirina/styles.css` `--color-primary-ink` now `color-mix(...75%, black)` with comment "DSD-06: category titles ... measured 4.37:1 ... AA-safe ink brings it to 6.88:1"; footer legal-link touch targets fixed on only 1 of 5 templates -> now `min-height: 24px` on the SHARED `.hb-legal-links a` in `bot/site-legal.js:675` (applies to all templates); no beforeunload/pagehide guard, silent keystroke loss on fast reload -> `builder/app.js:1359` "Save state — visible saving/saved/failed + exit guard (Wave 9)", comment cites "there was no beforeunload guard at all"; bulk photo manager unreachable on all 5 templates (`findPhotoPaths()` didn't recurse into arrays-of-objects) -> `builder/app.js:3481-3509` `findPhotoPaths()` now recurses, comment: "which is why 'Manage photos' used to render for no one"; broken `wave5-builder-undo-redo.test.js` oracle (wrong selector for local-service's bespoke list-add) -> test now uses `.hb-add-btn, .hb-ls-add`. Everything else in the doc is either already-passing coverage the branch reproduced independently, or explicitly flagged by the branch's own author as not-yet-confirmed/low-confidence (business-rename-cascade undo, memory-cap stress test, double-`<h1>` a11y-tree impact) rather than a stated defect. |
| worktree-agent-a9fef9db8df53e7a4 | SAFE TO DELETE | "fix(publish): canonical and og:url, robots and sitemap, provider timeouts, RO errors" (findings F5/F6/prof-06/DI-05/BE-06 from the original audit). Fully reimplemented on main: `bot/site-export.js` has canonical/og:url (F5) and robots.txt/sitemap.xml generation (F6, comments cite the same finding ids), `bot/deploy-cloudflare.js`/`deploy-vercel.js`/`revolut.js` all now use `AbortSignal.timeout(...)` with comments citing "DI-05" verbatim. Strongest evidence: `bot/test/audit-publish-seo.test.js` — the exact 397-line oracle this branch added — exists on main too (commit e22d775, "fix(payments): apply the four SEO-wave handoff items, close the dunning gaps"), and its own header comment says "Every assertion below was re-verified against TODAY's main ... not carried over blindly from the original branch this was ported from" and documents two allowlist/routing bugs (registry-shared.js `SITE_EXTRA_FIELDS`, `bot/web.js` dunning-webhook dispatch) that were fixed during the port — i.e. main's version is a corrected superset of this branch's, not a parallel reimplementation that might have drifted. |
| worktree-agent-aa1d745ac7fafe086 | SAFE TO DELETE | `04-QA-Evidence/Reaudit-2026-09-06/templates/FINDINGS.md`, a full owner-persona re-audit of all 5 templates. Every Critical/High/Medium finding is fixed on main, each with a commit explicitly labeled with this audit's own finding ids: desserdirina Critical (published menu showing one category's items under another's heading, originals missing) -> 52a0a1d/0c58119 "the category that vanished, and why it looked worse" (root-caused to `findListItemContainer()` in the shared overlay not stopping the climb on a list's last item); portfolio High ("+Adaugă" on `.pf-price` does nothing and transiently strips a sibling's delete control) -> c95751d "WAVE8-01 (portfolio, HIGH)", a double-digit list-index string-prefix collision in `builder/edit-overlay.js`, fixed generically for every list on every template, with a dedicated regression test `bot/test/wave8-templates-portfolio-pricing-add.test.js`; professionals High (WhatsApp floating button uncklickable, covered by header hit-test after a realistic edit session) -> e7ad920 "WAVE8-02 (HIGH)"; the cross-template Medium (200% CSS-zoom horizontal overflow on product-menu/portfolio/professionals, worst on desserdirina at 42%) -> dd17191 "WAVE8-03 (MEDIUM)" plus desserdirina-specific work in ae474c9; desserdirina's tiny/cramped gallery -> also fixed in ae474c9 ("gallery heading/CLS/zoom, itemShape"). The one item the branch itself flagged as unconfirmed/low-confidence (a product-menu 2-item category emptying, not reproducible outside the full multi-step run) was never elevated to a stated defect by the branch's own author and needs no resurrection. |
| worktree-agent-acf23fc9e0fab5e11 | HOLDS SOMETHING | `04-QA-Evidence/Reaudit-2026-09-06/export-docs-perf/FINDINGS.md` re-audited export/renderer, docs and performance. Most of its confirmed-fixed items check out on main (WhatsApp QR encoder, appointment-form honest failure on self-hosted export, portfolio icon-sink XSS bypasses, self-serve custom domains, performance now well under target). But 3 of its own newly-found defects are STILL PRESENT and undocumented elsewhere: **#A (Critical, scoped)** — `bot/webpublish.js`'s `predictedPublicOrigin()` (line ~1215-1231) returns `''` for isolated deploy with `PUBLIC_URL` unset, so `seoBuildOrigin` falls back to the literal placeholder `https://pending-deploy.hidook.invalid` (`PENDING_SEO_ORIGIN`, line 111); the post-deploy correction block (line ~1916-1927) only rewrites the pre-deploy `siteDir`, never the copy `_isolatedDeploy()` already `fs.cpSync`'d into `$DATA_DIR/published/<slug>/` moments earlier (the one `serveLive()` actually serves), and explicitly skips redeploying when `deployProvider === 'isolated'` — so a self-hosted isolated-deploy install that omits `PUBLIC_URL` ships the fake domain in canonical/og:url/robots.txt/sitemap.xml forever, with no self-healing on republish. Confirmed still true by tracing the exact code path; not exposed by documented production config (Cloudflare+BRAND_DOMAIN, a different branch). **#B (Medium, scoped)** — `bot/site-export.js:339` `originFromCanonical()` still does `new URL(canonical).origin`, which strips any path; a re-exported ZIP/HTML from an already-published isolated-deploy site (whose real canonical is `host:port/live/slug/`) still gets `robots.txt`/`sitemap.xml` pointing at the bare host with no path. **#D (High)** — `GO-LIVE.md:279` still says verbatim "Custom domains are concierge, not self-service" while `bot/domains.js` (877 lines) implements exactly the self-serve flow the doc claims doesn't exist — a launch-pricing decision document actively contradicting the shipped product. (#C, BACKUP-RESTORE.md staleness re: `scripts/ops-backup.js`, duplicates the same still-open finding already recorded under `worktree-agent-a210a955143021a0f` above — not double-counted as new.) |
| wt/advocate-eed3ca0-repair | SAFE TO DELETE | An early (pre-2026-09-04) fix branch for 3 stranger defects: local-service hero-tagline clip, non-Romanian legal `<title>` tags, and cookie banner covering hero/section titles. `builder/{cookies,privacy,terms}.html` are byte-identical to main already (`git diff` empty) — legal titles fixed. The cookie-banner-overlap fix is superseded by a far more elaborate, later rewrite: main's `bot/site-legal.js` `COOKIE_BANNER_CSS` is now a full "shared bottom-chrome layout" contract (bottom-LEFT card, `.ls-dock`/`.whatsapp-float` safe-zone variables, documented 44px-WCAG button history) that replaced this branch's simpler bottom-right-card approach entirely — the two are not compatible (main moved the banner to the opposite corner), so the branch's specific fix is not just present-by-another-route, it is architecturally obsolete. `templates/local-service/styles.css` on main independently carries every one of the branch's touch-target/contrast/CLS concerns plus much more (`.hb-ls-add`/`.hb-ls-remove` list controls, `color-mix()` contrast fixes, `aspect-ratio` CLS fixes) that this branch's older version of the same file doesn't even have yet. Its test `bot/test/advocate-eed3ca0-repair.test.js` hardcodes a personal machine's Brave path and playwright install location (not a portable oracle) and asserts bottom-RIGHT cookie-banner placement, which would now fail against main's bottom-LEFT contract — not a test worth resurrecting. |
| wt/audit-06-desserdirina-hero-bg | SAFE TO DELETE | One-line fix: `templates/desserdirina/template.html` hero background style changed from `background-image: {{& hero.background}}` to `background: {{& hero.background}}` (so a `linear-gradient(...), url(...)` overlay value renders instead of silently failing the `background-image`-only property). Main's current `template.html:74` has exactly this line already. The rest of the branch's diff against main is just staleness — this branch predates main's subsequent desserdirina rewrites (self-hosted fonts, page-sections restructuring, DSD-03 empty-category fix) and is not itself the source of any of that later work. Its test `bot/test/desserdirina-hero-background-edit.test.js` has no main-line counterpart by that name, but the single behavior it locks (hero background survives an edit) is exercised indirectly by main's own template rendering and the cross-template re-audit (`worktree-agent-aa1d745ac7fafe086`, already reported above) confirmed live that "the hero background no longer disappears after editing." |
| wt/audit-07-logout-invalidate | SAFE TO DELETE | "fix: AUDIT-07 logout invalidates session server-side" (537ad8b). This exact finding (AUDIT-07, also documented independently by `worktree-agent-a3a347f1a5502f4b2`'s re-audit above) is fixed on main by a later, different commit: 8c1b321 "fix(auth): logout revokes the session server-side, add log-out-everywhere", which explicitly cites "AUDIT-07 re-audit" in its own message and ships `/api/auth/logout` + `/api/auth/logout-everywhere` plus `revokeSession`/`revokeAllSessionsForUser` in `bot/auth.js`. Same defect, same finding id, reimplemented by a different, later commit — this branch's own version of the fix is superseded. |
| wt/caldiy-vision-decision | SAFE TO DELETE | Docs-only snapshot (2026-09-04) recording the owner's then-current decision: self-host cal.diy for the Professional calendar, in VISION.md §8 and PROJECT_STATUS.md. This decision was itself overridden the same day by a later owner decision ("NATIVE HIDOOK (LOCKED)"), which main's current VISION.md §8 documents explicitly, by name, including the fact that the cal.diy override existed and was superseded ("supersedează ... override-ul earlier same-day (2026-09-04) pentru self-hosted cal.diy"). The native-Hidook calendar this decision was replaced by is fully built on main (steps a-e: data model, booking engine, tenant isolation, public widget, owner dashboard, email harness, staged opt-in cutover) — far beyond this branch's forward-looking cal.diy architecture sketch, none of which was ever implemented (the branch itself says "niciun cod de produs schimbat pe acest card"). Nothing is lost: the historical fact is on record in main's own document, and the decision it recorded is moot. |
| wt/flow4-cal-clear-republish | HOLDS SOMETHING | 5 small Flow4 fixes bundled on one branch (2026-09-03), one per commit. (1) `7f8745a` iframe-repaint-via-`cloneNode` after trial return -> CONFIRMED on main (`builder/app.js:5906-5912`, same clone/replace pattern). (2) `a916e40` `Cache-Control: no-store` on live-site HTML in `serveLive()` so a browser never revisits a cached pre-republish page (e.g. one still showing a Cal.com CTA after the customer cleared that field) — `git log -S` for this exact line matches ONLY this branch's commit, nowhere in main's history; main's current `serveLive()` (rewritten later under "PERF-03") sets only `Content-Type`/`Content-Length`/`Vary`/`Content-Encoding`, no `Cache-Control` at all, with a comment saying ETag/Last-Modified were deliberately left out but saying nothing about `Cache-Control` — looks like an unintentional regression during the PERF-03 rewrite, not a considered removal. (3) `ed2ebc4` reserve editor space for the trial-success toast (`.builder-toast-visible` class + `.screen-edit { padding-top }`) so the toast can't cover editor controls — NOT on main; `builder/app.css`'s `.editor-preview-cookie-isolated .toast` rule is still exactly the pre-fix version (same top-of-file comment as before this branch's edit). (4) `ed2ebc4`/`7f8745a` Instagram-connect modal copy — main still shows the unconditional "Conectează Instagram din editor, înainte să începi trialul" even when `currentSitePaid` is true (`builder/app.js:4084`), telling an already-paying customer to do something "before starting the trial" — minor copy inaccuracy, not fixed. All test files on this branch hardcode a personal machine's Playwright path (`/Users/Work/.hermes/hermes-agent/node_modules/playwright`), so they are not portable oracles as-is. |
| wt/flow4-return-canvas-r12 | SAFE TO DELETE | Strict git ancestor of `wt/flow4-cal-clear-republish` (same commit lineage, an earlier checkpoint) — `git merge-base --is-ancestor` confirms it. Contains a subset of that branch's commits (`7f8745a`/`ed2ebc4`), already fully analyzed above under `wt/flow4-cal-clear-republish` (HOLDS SOMETHING there covers everything this branch has: the iframe-repaint fix is on main, the `no-store` cache fix and toast-reserves-space fix are not). Nothing here is unique to this branch specifically. |
| wt/flow4-toast-nav-clip | SAFE TO DELETE | Strict git ancestor of `wt/flow4-cal-clear-republish` (same commit lineage, an earlier checkpoint) — `git merge-base --is-ancestor` confirms it. Contains a subset of that branch's commits (`7f8745a`/`ed2ebc4`), already fully analyzed above under `wt/flow4-cal-clear-republish` (HOLDS SOMETHING there covers everything this branch has: the iframe-repaint fix is on main, the `no-store` cache fix and toast-reserves-space fix are not). Nothing here is unique to this branch specifically. |
| wt/of-20260902-details-open | SAFE TO DELETE | "fix(flow2): reopen details for each new design" (OF-3). Its test `bot/test/of3-details-new-design.test.js` exists on main byte-for-byte except a `BASE_SHA` comment pointing further along the same lineage (a448a3b, a later commit than this branch's own parent) — i.e. the branch's own oracle continued to track main and pass. `builder/app.js` has the auto-open logic ("True when Details should auto-open (first visit or last preference was open)", line 2719) and the feature is exercised throughout the FullPass evidence (`12/17/21/25/29-*-details-auto-open.png`). Diffstat against current main looks huge only because main's `app.js`/`app.css`/`index.html` have grown by thousands of lines since this 2026-09-02 branch point — normal branch-is-behind-main noise, not lost functionality. |
| wt/of-20260902-e2e-oracle | SAFE TO DELETE | "test(flow2): add binding template e2e oracle" — `bot/test/flow2-template-e2e-oracle.test.js` and `bot/test/flow2-template-e2e.mjs` both exist on main; the `.mjs` file is byte-identical, the `.test.js` differs only by a since-removed stale-oracle-reconcile comment. Fully present. |
| wt/of-20260902-empty-tab | SAFE TO DELETE | "fix(flow2): seed new repeatable items" — no test file by this exact name (`flow2-add-item-defaults.test.js`) survives on main, but the behavior it locks (repeatable list items get real Romanian starter text, e.g. "Specialitate nouă"/"Serviciu nou"/"Pas nou"/"Categorie nouă", never an empty card) is implemented in `builder/app.js`'s `onListAdd()` (line 2364) and `builder/edit-overlay.js`, and has been independently re-confirmed live across every template by multiple later re-audits already covered above (e.g. `worktree-agent-aa1d745ac7fafe086`'s cross-template pass: "never a blank/empty item... across product-menu, local-service, professionals"). Redundant coverage of an already-fixed, already-verified behavior, not a loss. |
| wt/of-20260902-ogimage | SAFE TO DELETE | "fix(flow2): derive social image from site photos" (OF-1) — `bot/test/of1-og-image-oracle.test.js` on main is identical except a `PARENT_SHA` bump to a later commit in the same lineage; `build.js`'s `deriveSocialImage()` (line 256) and its use for `cfg.seo.ogImage` (line 310) are present on main. |
| wt/of-20260902-whatsapp | SAFE TO DELETE | "fix(flow2): center and polish WhatsApp QR panels" — `bot/test/flow2-whatsapp-panel-polish.test.js` on main is identical except a `BASE_SHA` bump; the `.wa-qr`/`.wa-qr__*` polished panel classes it tests for are present in `templates/professionals/styles.css` (and the other 4 templates). |
| wt/of6-onto-main | SAFE TO DELETE | Same lineage/content as `wt/of-20260902-empty-tab` (identical `flow2-add-item-defaults.test.js` content, just a later `BASE_SHA`) — see that branch's entry above; the behavior is implemented and independently re-verified on main. |
| wt/s43-telegram-help-no-publish | HOLDS SOMETHING | "feat(S43): leftover Telegram /help must not promise live publish" (2026-08-23) — fixes `bot/flow.js`'s `handleHelp()` copy, which told a Telegram user "Plătești ... → site LIVE" as if paying inside Telegram published the site live, when publish actually happens in the Hidook web builder. Main's current `bot/flow.js:1613-1618` still has the UNFIXED text verbatim ("site LIVE pe HTTPS" implying Telegram-driven publish) — this branch's fix never landed. WANTED status: not urgent to resurrect right now — Telegram is frozen product-wide since 2026-09-06 ("niciun fișier Telegram nu se mai atinge", VISION.md), and the freeze note itself says Telegram audit findings are explicitly deferred, not resolved, and don't count against product evaluation. This is exactly that kind of deferred item: a real, small, already-written honesty fix for frozen code, worth applying the day Telegram unfreezes but not before. |
| wt/s45-apple-site-systems | SAFE TO DELETE | "feat(S45): restyle three site systems with Apple principles" (2026-08-23) — a full visual redesign of local-service/portfolio/product-menu toward a black/#f5f5f7/blue-accent "Apple" aesthetic with "Kowalski motion." Main shipped a completely different, deliberate design direction for every one of these templates instead: local-service is now a "Navy trust + orange-accent CTAs... construction/trade vertical," portfolio is now "Beauty / salon / spa vertical... Powder calm, gallery-first." Confirmed by comparing the top-of-file design-intent comments and `:root` color tokens directly — not a refinement of this branch's direction, a different one entirely. This is an abandoned aesthetic exploration superseded by a later, shipped design choice, not a loss — nothing about the current templates' extensively-audited accessibility/contrast/CLS work (covered by many branches above) depends on or references this branch's styling. |
| wt/s72-legal-oracle-ro | SAFE TO DELETE | "test: accept Romanian legal page titles in wave7 oracle" — refactors `bot/test/wave7-legal-pages.test.js`'s title/h1 check into separate per-page `titleMatcher`/`h1Matcher` regexes. Main's current version of the same file already accepts Romanian titles via a simpler `topicRe: /Termeni|Terms/i` (etc.) pattern, with a comment crediting `advocate-eed3ca0` for the RO-title fix — same behavior (RO legal titles pass), different, simpler implementation already in place and passing. The branch's refactor adds no coverage the existing regex doesn't already provide. |
| wt/sec-01-auth-devlink | SAFE TO DELETE | "fix: fail closed auth email without Resend in production" — guards against minting/returning a raw-bearer-token `devLink` when `NODE_ENV=production` and `RESEND_API_KEY` is unset. Main's current `bot/server.js:1298-1312` (`handleAuthEmail`) has the equivalent, more thorough fix: it logs `server.auth.email.no_provider_in_production` and skips the send entirely in that case, AND separately gates the response's `devLink` field on `NODE_ENV !== 'production'` regardless — double-guarded against exactly this leak, confirmed independently by `worktree-agent-a3a347f1a5502f4b2`'s backend-security re-audit above ("Auth dev-link fallback (original CRITICAL) — correctly fixed"). |
| wt/sitebuilder-flow1-foundation | SAFE TO DELETE | Early (2026-08-28) docs sync of VISION.md/PRODUCT.md/AGENTS.md against owner feedback. Main's VISION.md has been rewritten and re-synced multiple times since (last sync recorded 2026-09-04 plus numerous dated owner-decision overrides layered in afterward, covered elsewhere in this report) — strictly ahead of this snapshot. Spot-checked `00-Governance/OWNER-FEEDBACK-2026-08-26-SPEC.md`: main keeps the FULL original evidence-backed spec (file:line citations, real browser observations, reproduction commands), while this branch's version replaces it with a short pointer stub ("Full original content is stored in [a Kanban attachments path]... use VISION.md for current truth") — main's copy is strictly more complete, not less; merging this branch would have been a regression, not a gain. |
| wt/xss-01-portfolio-icon | SAFE TO DELETE | "fix: harden SVG icon URL entity handling" — an early version of the portfolio icon-sink XSS fix (`sanitizeSvgUrl`, decoding numeric + a fixed set of named entities: Tab/NewLine/amp/apos/gt/lt/quot). Notably this version does NOT decode `&colon;`, the exact bypass `worktree-agent-a3a347f1a5502f4b2`'s re-audit later found and reported (C2 above) — so this branch's fix was itself incomplete. Main's current implementation (`normalizeUrlForSchemeCheck()`) is a strictly more thorough rewrite: it decodes numeric AND a larger named-entity table (including `colon`), repeats decoding until the string stops changing (closing nested-encoding gaps this branch's single-pass version couldn't), and is the actual fix for the C2 finding confirmed above. Verified by extracting this branch's own 3 test cases (`bot/test/engine.test.js`, not present on main) and running them directly against current `build.js`/`renderHtml()`: all 3 pass unmodified — main's superseding implementation is a strict superset of what this branch's tests check. |

## Notes

- `git branch --no-merged main` returned 26 refs, not the 25 the task quotes.
  All 26 are audited below.

## Still wanted? (every HOLDS SOMETHING branch)

**`worktree-agent-a210a955143021a0f`** (BACKUP-RESTORE.md staleness + eager-DB-open readiness gap):
WANTED, low effort. The doc fix (point `BACKUP-RESTORE.md` at `scripts/ops-backup.js`/
`ops-restore.js` instead of claiming they don't exist) is a same-day-safe, no-risk
documentation edit an operator could need at 3am during a real incident — worth doing
regardless of anything else. The eager-DB-open item is a smaller, architectural nice-to-have
(make `bot/web.js`'s startup path lazy like `bot/server.js`'s own `getRegistry()`) — real but
lower priority, since the current behavior (crash-loop on corrupted DB at boot) is an
acceptable, if narrower, failure mode.

**`worktree-agent-acf23fc9e0fab5e11`** (isolated-deploy-without-PUBLIC_URL SEO placeholder,
export path-stripping, GO-LIVE.md custom-domain staleness): PARTIALLY WANTED. The GO-LIVE.md
fix (#D — stop telling a future owner custom domains are concierge-only when self-serve has
shipped) is WANTED and cheap: it actively misinforms a launch-pricing decision. The #A/#B
code defects are real but scoped to a self-hosted-isolated-deploy topology the actual shipped
production config (Cloudflare+BRAND_DOMAIN) does not use — worth fixing before anyone
self-hosts this product via isolated deploy without setting PUBLIC_URL, but not urgent for
the current single-tenant production deployment. Not an oracle for a dead defect — the code
path still exists and is reachable — just not on the hot path today.

**`wt/flow4-cal-clear-republish`** (no-store cache header on live HTML, toast reserving editor
space, Instagram-modal copy for paid sites): MIXED. The `Cache-Control: no-store` regression is
WANTED — it looks like an accidental drop during a later performance rewrite (PERF-03) rather
than a deliberate choice, and its whole purpose (never let a browser show a stale post-republish
page) is still exactly as relevant today. The toast/editor-space and Instagram-copy items are
minor polish, worth a look but not urgent — nobody has re-reported them since 2026-09-03 despite
several later builder-UX audits (`worktree-agent-a61ab6ab06e1bd9f0` above) exercising toasts and
modals without flagging either.

**`wt/s43-telegram-help-no-publish`** (Telegram `/help` still claims Telegram-driven publish):
NOT WANTED RIGHT NOW. Telegram has been explicitly frozen product-wide since 2026-09-06
("niciun fișier Telegram nu se mai atinge") and its audit findings are deliberately deferred,
not resolved, per VISION.md's own words. Applying this fix today would touch a frozen file
against explicit owner policy. Worth keeping the branch's diff (it's a 2-line, already-correct
fix) as a reference for whenever Telegram thaws, but don't resurrect it into a PR now.

---

## Ce s-a făcut cu constatările (autorul, 2026-09-07)

Cele patru ramuri „HOLDS SOMETHING" au fost verificate independent pe `main`,
reproduse, și reparate. Nicio ramură nu a fost ștearsă — decizia rămâne a
proprietarului, iar acest raport e temeiul ei.

| constatare | verificare | reparație |
|---|---|---|
| `Cache-Control` pierdut pe site-urile publicate | reprodus: zero antete de cache, zero validatori | `2a81ece` — `no-cache` + `ETag`, revalidare la 304 |
| canonical către `pending-deploy.hidook.invalid` | reprodus în fișierele **servite**: 2 + 1 + 4 apariții | `efa78e1` — nu se mai afirmă o origine când nu există una |
| `BACKUP-RESTORE.md` neagă uneltele care există | rulat `ops-backup.js`: a făcut un instantaneu real | `4287253` — manualul le documentează; golul real (programarea) e numit |
| `GO-LIVE.md`: domenii „concierge" | `bot/domains.js`, 877 linii, flux self-serve în editor | `4287253` |
| sitemap-ul unui re-export listează rădăcina gazdei | reprodus: 4 `<loc>` greșite | `bfcae8e` — baza păstrează calea din canonical |

Constatarea despre Telegram (`wt/s43-telegram-help-no-publish`) rămâne
**deliberat nereparată**: Telegram e înghețat prin decizia proprietarului din
2026-09-06. E o corectură reală, mică, deja scrisă, de aplicat în ziua în care
se dezgheață.

Deschiderea din `worktree-agent-a210a955143021a0f` — `registry-sqlite.js` deschide
baza la încărcarea modulului, deci `/health/ready` nu poate raporta nimic dacă
baza e coruptă la pornire — **nu a fost reparată**. E un compromis de tip
fail-fast defendabil sub politica de repornire a Railway, dar îngustează o
garanție pe care documentația o formulează mai larg. Merită o decizie explicită,
nu o reparație grăbită la 5 dimineața.
